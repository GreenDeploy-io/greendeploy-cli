# To update requirements.txt, run:
#
#    source ~/.venv/greendeploy-py3101/bin/activate
#    cd ~/Projects/greendeploy
#    pip-compile requirements.in
#
# If not installed, `pip install pip-tools`
#
# To install in localhost, run:
#
#    pip-sync requirements.txt
#

# Click
# ----------------------------------------------------------------
click==8.0.0  # 2021 Dec 7 https://docs.djangoproject.com/en/3.2/releases/3.2.10/

pyfiglet==0.7.2

cookiecutter~=1.7.0

gitpython~=3.0
python-json-logger~=2.0
pip==21.2.4 # must use this else it will fail with pip-tools
pip-tools==6.4.0 # must use this else it will fail with pip-tools
PyYAML>=4.2, <6.0